.references {
    font-size: $font-size-base;
    font-style: italic;
    text-align: right;
}

p, dd, address {
    font-family: $font-family-serif;
}

h1, h2, h3 {
    font-weight: 900;
    line-height: floor($line-height-doubled * 1.33);
}
